query ClipConnection(
  $first: Int
  $last: Int
  $before: String
  $after: String
  $query: String
  $orderBy: Ordering
) {
  clipConnection: clips(
    first: $first
    last: $last
    before: $before
    after: $after
    query: $query
    orderBy: $orderBy
  ) {
    edges {
      node {
        id
        name
        createdAt
        updatedAt
        tags
        lastEditAt
        sql
      }
    }
    pageInfo {
      startCursor
      endCursor
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
